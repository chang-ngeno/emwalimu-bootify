/*
 * Created on $today.date ( Time $today.time )
 * Generated by $generator.name ( version $generator.version )
 */
#set ( $uncapitalizedEntityName = ${fn.uncapitalize($entity.name)})
##
#set ( $entityAttribs = "" )
#foreach( $attrib in $entity.attributes )
#if ( $foreach.count > 1 )
#set ( $entityAttribs = $entityAttribs + ", " )
#end
#set ( $entityAttribs = $entityAttribs + ${fn.uncapitalize($attrib.name)} )
#end

/**
 * $entity Entity (ES6 Class)
 */

module.exports = class $entity {
    constructor(${entityAttribs}) {
#foreach ( $attrib in $entity.attributes)
#set ( $uncapitalizedAttribName = ${fn.uncapitalize($attrib.name)})
 		this.${uncapitalizedAttribName} = ${uncapitalizedAttribName};
#end
    }
};