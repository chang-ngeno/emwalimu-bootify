/*
 * Created on $today.date ( Time $today.time )
 * Generated by $generator.name ( version $generator.version )
 */

// Load Modules
const express = require('express');
const router = express.Router();

// Load controller
#foreach ($entity in $selectedEntities)
const ${entity.name}Controller = require('../../controller/${entity.name}Controller');
const ${fn.uncapitalize($entity.name)}Controller = new ${entity.name}Controller();

#end

/**
 * Count API routes
 */

#foreach ($entity in $selectedEntities)
#set ( $uncapitalizedEntityName = ${fn.uncapitalize($entity.name)})
router.get('/${uncapitalizedEntityName}.count', function (req, res) {
    ${uncapitalizedEntityName}Controller.countAll(res);
});

#end

const CountRoutes = router;

module.exports = router;